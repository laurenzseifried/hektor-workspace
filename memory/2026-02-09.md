# 2026-02-09 — Tag 1: Bootstrap & Config

## ✅ Erledigt
- Workspace-Files geschrieben (SOUL, USER, AGENTS, TOOLS, IDENTITY, HEARTBEAT, MEMORY)
- Scout Workspace komplett mit SOUL, USER, AGENTS, TOOLS, IDENTITY, HEARTBEAT
- Ollama läuft (llama3.2:3b geladen)
- clawhub installiert
- cisco-ai-skill-scanner installiert (mit --break-system-packages)
- Skills installiert: clawdex ✅, self-improving-agent ✅
- Config Patch erfolgreich:
  - Model Routing (haiku primary, gemini+ollama fallback)
  - Multi-Agent setup (hektor + scout)
  - Telegram: 2 Accounts (hektor + scout) mit Bindings
  - Memory Search (local provider, gemini fallback, hybrid query)
  - Heartbeat (30min, ollama, telegram)
  - Compaction (reserveTokensFloor 20k, memoryFlush enabled)
  - Session reset (daily 4am, idle 120min)
  - Browser enabled
- Gateway restartet + config-apply erfolgreich

## 🔄 In Progress
- proactive-agent Skill: `halthelobster/proactive-agent` nicht auf clawhub gefunden — muss anders heißen

## ⏳ Noch offen
- [ ] proactive-agent finden + installieren (Laurenz sagt es gibt ihn von halthelobster)
- [ ] Dashboard klonen (blockiert: SSH Key Passphrase)
- [ ] Git Repo für Workspace-Backup erstellen
- [ ] Crons einrichten (4 Jobs: Token Audit, Backup, Morning Briefing, Weekly Maintenance)
- [ ] Ollama Watchdog (launchd plist)
- [ ] Firewall prüfen (ollama auf 127.0.0.1 only)
- [ ] Hooks aktivieren (session-memory, boot-md)
- [ ] Smoke Tests (alle Checklisten durchgehen)
- [ ] BOOTSTRAP.md löschen

## Blockiert
1. **SSH Key Passphrase** — `ssh-add ~/.ssh/id_ed25519` braucht Passphrase-input. Kann nicht automatisiert werden ohne SSH_ASKPASS.
2. **proactive-agent Skill** — Laurenz sagt halthelobster hat ihn, aber nicht unter `halthelobster/proactive-agent` auf clawhub. Vielleicht anderer Name oder noch nicht veröffentlicht.

## Kontext
- Scout ist jetzt auch als Agent konfiguriert (eigener Workspace, Haiku, Telegram account)
- Zweiter Bot-Token (SCOUT_BOT_TOKEN) in .env vorhanden
- Skill-Scanner kann LLM nicht laden (SKILL_SCANNER_LLM_API_KEY nicht gesetzt), nutzt behavioral analyzer
- Konfiguration ist modular via config.patch erfolgt (OK: mehrere restart-Zyklen)
